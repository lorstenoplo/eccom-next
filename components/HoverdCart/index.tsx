import { FC } from "react";
import useStyles from "./mui-styles";

type Props = {};

const HoverdCart: FC<Props> = () => {
  const classes = useStyles();
  return <h1>cart</h1>;
};

export default HoverdCart;
